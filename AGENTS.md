## 给 Agent

1. 本目录是公开交付仓的完整交付树，存放五家 Agent 的插件清单、注入入口、安装脚本和经批准的 skills 镜像；研发仓的源稿、研发记录、归档和临时内容不放在这里；
2. 先读同级目录的 [`README.md`](./README.md)，只打开本次任务涉及的文件；
3. 禁止无差别读取全部项目，避免占用上下文。

## 开工顺序

1. 同级 `README.md` 与本文件入口完整；不完整时只修入口，不修改交付内容；
2. `build/skills/` 只能由发布流程从研发仓 `skills/` 全量镜像（release-superextensions skill 的 stage 命令），禁止在镜像内直接编辑或反向覆盖源稿；
3. 公开仓推送只能走发布流程的 candidate/publish 命令，禁止手工拼装上传或直接 git push 公开仓；
4. 最终文件齐全不能抵消顺序违规。

## 变更联动

skills/ 源稿新增、移除或改名后，除运行 stage 同步镜像外，同一次提交里核对所有登记处：根 `README.md` 的 skill 提及、`build/README.md` 工具表、各 Agent 插件清单——本目录曾发生工具表漏更事故，此为强制检查项。

## 重建

本目录内容可从研发仓整体重建：`skills/` 由 stage 全量镜像恢复；其余交付适配文件（清单、hooks、脚本）直接在 `build/` 维护，研发仓即其唯一正稿来源。公开仓内容随时可从本目录重建，公开仓不承担备份职责。

## 维护原则

1. 条目信息只在 `README.md` 维护，本文件不复制索引、动态数量或单项详情；
2. 不把本次使用的命令、工具、脚本和临时选择写成长期要求；
3. 上级 `AGENTS.md` 只增加本目录的触发条件和本文件链接，不得覆盖或重写本文件；
4. 控制本文件大小，防止小窗口模型撑爆上下文；只删重复、动态和一次性内容，不砍必要规则。

## 版本控制

1. 本目录治理文件（`README.md`、`AGENTS.md`）由本项目自己的仓库跟踪；
2. 本目录属于研发仓的一部分，随研发仓版本控制；公开仓是本目录的对外分发目标，两条 Git 历史独立，只能经发布流程单向同步。

## 维护结果验收

每次操作后，向用户报告前重新读取实际文件，逐项确认：

1. 收录内容符合本目录边界，无混放；
2. 开工顺序中的红线得到执行，有操作记录可查，不是事后补齐；
3. `README.md` 索引与磁盘一致，链接可打开；
4. 本文件与 `README.md` 各司其职、互相可达，空上下文 Agent 仅凭本文件能安全维护本目录。
