# <项目名>

## 项目概览

<两三句：这是什么项目、给谁用、当前阶段；影响 Agent 判断的关键约束写在这里。>

## 项目地图

<列全部一级目录：有局部入口的链过去，没有的标 —。>

| 目录 | 入口 | 职责 |
|---|---|---|
| <目录名>/ | [`AGENTS.md`](./<目录名>/AGENTS.md) | <一句话职责> |
| <目录名>/ | — | <一句话职责> |

## 开工顺序

1. 先读本文件和项目 `README.md`，确认任务边界和本次任务要动的内容；
2. 按任务命中情况，读取对应目录的局部入口或场景规范；只读本次任务需要的；
3. 动手前确认每个要改的文件归属哪个目录、什么生命周期；归属判断不了先问，不得先写入再整理。

## 硬红线

1. <本项目特有的不可越线项；没有就只留下面通用三条。>
2. 密钥、凭据等敏感信息不入库、不写进对外产物。
3. 删除、覆盖、对外发布前向用户确认影响范围。
4. 治理状态与磁盘分叉时不掩盖，先摆出问题再收工。

## 维护规则

<本项目长期有效的规则与场景规范路由；每条一行，链接到正稿：>

- <规则主题>：见 <规范文件或路径>。
